{
  "name": "korpholmen",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm --prefix apps/matrikel test && npm --prefix apps/batregister test && npm --prefix apps/fastigheter test && npm --prefix apps/dokumentarkiv test",
    "build": "npm --prefix apps/matrikel run build:publish && npm --prefix apps/batregister run build:publish && npm --prefix apps/fastigheter run build:publish && npm --prefix apps/dokumentarkiv run build:publish"
  }
}
