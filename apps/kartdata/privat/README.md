# Privat arbetsyta

Innehållet här är ignorerat av Git. `kallkopior/kartdata-source.json` är en
bytebevarande JSON-extraktion av arbetsboken. `migrering-2026-08-03/` skapas
reproducerbart av `npm run build:migration` och innehåller startoperationer,
manifest och importkontroll.
